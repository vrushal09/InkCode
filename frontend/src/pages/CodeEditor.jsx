// Import the refactored CodeEditor container
import { CodeEditorContainer } from "../components/CodeEditor";

const CodeEditor = () => {
    // The component is now just a wrapper for the refactored container
    return <CodeEditorContainer />;
};

export default CodeEditor;